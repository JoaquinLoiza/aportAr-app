export interface Institution {
    image: string;
    name: string;
    location: string;
    presentation: string;
    collected: number;
    description: string;
    links: Array<string>;
    quantity: number;
}